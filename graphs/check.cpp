#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> v;
    auto v2 = vector<int>();
    v2.push_back(2);
    v2.push_back(2);
    v2.push_back(2);
    v2.push_back(2);
    v2.push_back(2);
    v2.push_back(2);
    v2.push_back(2);

    cout<<sizeof(v)<<" n "<<sizeof(v2)<<endl;
    return 0;
}